import { FontPlusTwentyDirective } from './font-plus-twenty.directive';

describe('FontPlusTwentyDirective', () => {
  it('should create an instance', () => {
    const directive = new FontPlusTwentyDirective();
    expect(directive).toBeTruthy();
  });
});
